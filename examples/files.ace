module outside {
    g: String = 'hello!'
    module nested {
        print 'hello from the other side'
    }
}

module files {
    import '../acelib/io'

    module nested {
        a: Int = 45
    }

    print nested::a
    print outside::g

    my_file = io::open('test.txt', 'w+')
    io::write(my_file, 'Ace version ', runtime::version[0], '.', runtime::version[1], '.', runtime::version[2], '\n')
    io::write(my_file, 'Running on OS: ', runtime::os_name)
}