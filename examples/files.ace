module files {
    import '../acelib/io'

    my_file = io::file::open('test.txt', 'w+')

    io::file::write(my_file, 'Ace version ', 
        runtime::version[0], '.', 
        runtime::version[1], '.', 
        runtime::version[2], '\n\n\n')

    io::file::write(my_file, 'Running on OS: ', runtime::os_name)
}