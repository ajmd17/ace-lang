module functions {
    let log = (what, args...) {
        print what
        print 'args = ', args
    }

    log('test',4,3,4,43)


    // simple function to take 'name' as an input,
    // and make use of it.
    
    say_name: Function = (name) {
        print "Hello, ", name, "!"
    }

    print say_name

    say_name("your name here")
}